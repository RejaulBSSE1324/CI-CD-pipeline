{
  "name": "ci-cd-pipeline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "ng test",
    "test:prod": "ng test --browsers=ChormeHeadless --watch=false --code-coverage"
  },
  "dependencies": {
    "ng": "^0.0.0",
    "preact": "^10.22.0",
    "test": "^3.3.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "vite": "^5.3.1"
  }
}
